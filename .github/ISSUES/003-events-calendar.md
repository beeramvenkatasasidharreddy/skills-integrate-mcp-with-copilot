# Add events and calendar support

Add event models and calendar import support.

Tasks:
- Add `Event` and `EventShowing` models with types and multiple showings.
- Support ICS import to sync external calendars.
- Add event listing and event detail endpoints.

Labels: feature, backend
