# Implement membership roles and permissions

Add role-based membership system to support owners, officers, and members, plus invites and requests.

Tasks:
- Add `Membership` model with role enum (owner/officer/member).
- Implement membership invites and request endpoints.
- Implement permission checks for protected actions.

Labels: feature, backend
